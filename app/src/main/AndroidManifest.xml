<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upitnik.playwithlessons">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_playwithlessons"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_playwithlessons"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayWithLessons">
        <activity android:name="com.upitnik.playwithlessons.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>