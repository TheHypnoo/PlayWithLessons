<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.upitnik.playwithlessons">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_playwithlessons"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_playwithlessons"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlayWithLessons"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.relate.ActivityRelate"></activity>
        <activity android:name=".ui.questions.ResultActivity" />
        <activity android:name=".ui.questions.ActivityQuestions" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>